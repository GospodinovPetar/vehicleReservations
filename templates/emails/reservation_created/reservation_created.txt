Reservation received: {{ reference }}

Hi {{ group.user.get_full_name|default:group.user.username }},


Dates: {{ reservation.start_date }} → {{ reservation.end_date }}
Pickup: {{ reservation.pickup_location.name }} | Return: {{ reservation.return_location.name }}
Status: {{ reservation.get_status_display }}
Details:
- Vehicle: {{ reservation.vehicle }}
- Dates: {{ reservation.start_date|date:"Y-m-d" }} → {{ reservation.end_date|date:"Y-m-d" }}
- Pickup → Return: {{ reservation.pickup_location }} → {{ reservation.return_location }}
- Status: {{ reservation.group.get_status_display|default:reservation.group.status }}{% if reservation.total_price %}
- Estimated total: €{{ reservation.total_price|floatformat:2 }}{% endif %}

We received your reservation {% if reservation.group and reservation.group.reference %}{{ reservation.group.reference }}{% elif reservation.group %}#{{ reservation.group.pk }}{% else %}#{{ reservation.pk }}{% endif %}.

Details:
- Vehicle: {{ reservation.vehicle }}
- Dates: {{ reservation.start_date|date:"Y-m-d" }} → {{ reservation.end_date|date:"Y-m-d" }}
- Pickup → Return: {{ reservation.pickup_location }} → {{ reservation.return_location }}
- Status: {{ reservation.get_status_display|default:reservation.status }}{% if reservation.total_price %}
- Estimated total: €{{ reservation.total_price|floatformat:2 }}{% endif %}
Status: {{ status }}

Items:
{% for r in items %}
- Vehicle: {{ r.vehicle }}
  Dates: {{ r.start_date|date:"Y-m-d" }} → {{ r.end_date|date:"Y-m-d" }}
  Pickup → Return: {{ r.pickup_location }} → {{ r.return_location }}
{% empty %}
- (No items)
{% endfor %}


You can review your reservation here:
{% url 'inventory:reservations' %}
